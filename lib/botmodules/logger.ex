defmodule BotLogger do
  @behaviour BotModule
  @moduledoc "Log all messages."

  def doc, do: @moduledoc

  def start() do

  end

  def process(_message) do

  end

  def stop(_reason) do

  end
end
